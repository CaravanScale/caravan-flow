// ServiceProvider — shared services available to all processors.
// Constructor-injected at processor creation time, not passed per-call.

// ServiceProvider holds cross-cutting services that processors may need.
class ServiceProvider {
    ContentStore contentStore

    init(ContentStore contentStore) {
        this.contentStore = contentStore
    }

    pub fn getContentStore(): ContentStore {
        return contentStore
    }
}
