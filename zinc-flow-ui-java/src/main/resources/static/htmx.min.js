// HTMX placeholder — slice 2 vendors the real htmx.min.js (v1.9+). Slice 1
// ships an empty stub so the <script src> tag resolves 200 without pulling a
// binary dep prematurely. Every hx-* attribute is ignored until then.
